Sources for the plugin `plugin.keyboardEvents`.

Add following to your `build.settings` to use:
```lua
{
    plugins = {
        "plugin.keyboardEvents" = {
            publisherId = "net.shakebrowser",
        },
    },
}
```
